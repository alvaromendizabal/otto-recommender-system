# OTTO Round 09 — Directed historical-session continuation

**24 primary features · 24 ablation features · fixed 134-feature control · twelve maximum new challenger fits.**

Read `START_HERE.md`. Use the existing project interpreter and persistent space; no package installation is required or attempted. Review `00_round07_review.ipynb` and the executed synthetic `04_feature_examples.ipynb`; run `01_build_features.ipynb` and `02_run_comparison.ipynb` cell by cell. Read the nine saved plots in `03_saved_results.ipynb` after successful reporting.

`RESEARCH_PLAN.md` specifies every formula, source rule, diagnostic and advancement gate. `feature_catalog.json` enumerates all 48 named candidate features across the two arms. `protocol.json` freezes the model/data limits. `notebook_sources.json` protects notebook code while allowing saved outputs. `MANIFEST.sha256` checks all package bytes; notebook source is checked separately after execution.

No private-data model score is included in this package. Round07 is a completed source-support pilot; its oracle is not achieved ranking. GitHub, AWS and Kaggle are not modified automatically. Shared extraction belongs to Round08 and is read-only for Round09; neither round inherits the other's result.
